'use strict';

const getAuthToken = {
  id: 'getAuthToken',
  properties: {
    query:{
      properties: {

      },
      additionalProperties: false
    }
  }
};

module.exports = { getAuthToken };

'use strict';

module.exports = {
  database: {
    password: '' // set a good, strong password
  },
  jwt: {
    secret: 'someL0ngandsecureSecretsTRing' // set a very good, long, strong secret. change it once in a while
  }
};
